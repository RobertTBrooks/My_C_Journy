/* * TITLE: Project 09 - Minimalist Phonebook
 * OBJECTIVE: Group different data types into one "Object" (Struct).
 * * MAN PAGES TO READ:
 * 1. 'man 3 strncpy' -> A safer way to copy strings than 'strcpy'.
 *
 * TO-DO HINTS:
 * 1. Define a struct 'Contact' with 'name' and 'phone' char arrays.
 * 2. Create an instance: struct Contact c1;
 * 3. Assign values using strncpy(c1.name, "Alice", 50);
 * 4. Print using the dot operator: c1.name and c1.phone.
 */

#include <stdio.h>
#include <string.h>

int main() {

    return 0;
}