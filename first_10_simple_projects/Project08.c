/* * TITLE: Project 08 - The Dynamic Array
 * OBJECTIVE: Learn to request memory at runtime, not just at compile time.
 * * MAN PAGES TO READ:
 * 1. 'man 3 malloc' -> Note that it returns a 'void *' (generic pointer).
 * 2. 'man 3 free'   -> Use this to return memory to the OS.
 *
 * TO-DO HINTS:
 * 1. Ask user: "How many numbers?" -> store in 'n'.
 * 2. int *arr = malloc(n * sizeof(int));
 * 3. Always check: if (arr == NULL) { exit(1); }
 * 4. Fill the array with a loop, print it, then call free(arr).
 */

#include <stdio.h>
#include <stdlib.h>

int main() {

    return 0;
}